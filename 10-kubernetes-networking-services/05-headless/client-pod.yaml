apiVersion: v1
kind: Pod
metadata:
  name: headless-dns-client
spec:
  containers:
    - name: tools
      image: alpine:3.20
      command:
        - sh
        - -c
        - |
          apk add --no-cache curl bind-tools >/dev/null
          sleep 3600
