# Section 08 Screenshot Checklist

Required genuine screenshots:

1. `01-version-check.png` — `minikube version` and `kubectl version --client`.
2. `02-minikube-start.png` — successful `minikube start`.
3. `03-minikube-status.png` — `minikube status` and `kubectl get nodes -o wide` showing a Ready node.
4. `04-minikube-stop.png` — `minikube stop` followed by stopped status.

Use your own terminal output only.
